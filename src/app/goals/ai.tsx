import { ModuleNoorAIScreen } from '@features/modules/noor-ai/module-noor-ai-screen';

/** `/goals/ai` — Noor AI, opened from goals (issue #64, Stage 1). */
export default function Screen() {
  return <ModuleNoorAIScreen moduleId="goals" />;
}
