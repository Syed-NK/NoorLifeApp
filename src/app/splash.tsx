import { SplashScreen } from '@features/entry-auth/screens/splash-screen';

/** Screen 01 — Splash. Reachable directly for validation; normally shown by the entry gate. */
export default function Screen() {
  return <SplashScreen />;
}
