import { QuranSelectionScreen } from '@features/faith/screens/quran-selection-screen';

/** Faith → Tasbih → Change → Add a Quran selection, and Faith → Duas → Add a selection. */
export default function Screen() {
  return <QuranSelectionScreen />;
}
