import { WorshipScreen } from '@features/faith/screens/worship-screen';

/** Faith → Worship. The `worship` bottom-navigation slot. */
export default function Screen() {
  return <WorshipScreen />;
}
