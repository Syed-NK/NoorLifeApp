import { ContentInfoScreen } from '@features/faith/screens/content-info-screen';

/** Faith → More → About this content. Where Faith's attribution lives. */
export default function Screen() {
  return <ContentInfoScreen />;
}
