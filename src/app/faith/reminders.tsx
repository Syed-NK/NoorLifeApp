import { PrayerRemindersScreen } from '@features/faith/screens/prayer-reminders-screen';

/** Faith → Prayer Times → Prayer reminders. */
export default function Screen() {
  return <PrayerRemindersScreen />;
}
