import { ChangeEmailScreen } from '@features/profile/screens/change-email-screen';

/** Change Email (Phase 6C-3A). Nested under Privacy & Security, in the Profile stack. */
export default function Screen() {
  return <ChangeEmailScreen />;
}
