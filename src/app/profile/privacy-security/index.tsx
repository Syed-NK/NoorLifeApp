import { PrivacySecurityScreen } from '@features/profile/screens/privacy-security-screen';

/** Privacy & Security (workflow §14, Phase 6C-3A). Replaces the centralized "Coming later" note. */
export default function Screen() {
  return <PrivacySecurityScreen />;
}
