import { SimplePlaceholderScreen } from '@features/placeholder/simple-placeholder-screen';

export default function Screen() {
  return (
    <SimplePlaceholderScreen
      title="Appearance & Accessibility"
      description="Theme, text size, reduced motion, high contrast and focus indicators."
      specReference="Workflow §14 · Design spec §8"
    />
  );
}
