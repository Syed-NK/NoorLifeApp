import { ManageSubscriptionScreen } from '@features/subscription/screens/manage-subscription-screen';

/** Screen 09 — Manage Subscription (Phase 5 §5.09). */
export default function Screen() {
  return <ManageSubscriptionScreen />;
}
