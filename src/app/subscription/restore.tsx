import { RestorePurchasesScreen } from '@features/subscription/screens/restore-purchases-screen';

/** Screen 08 — Restore Purchases (Phase 5 §5.08). */
export default function Screen() {
  return <RestorePurchasesScreen />;
}
