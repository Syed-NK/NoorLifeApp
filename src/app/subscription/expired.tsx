import { SubscriptionExpiredScreen } from '@features/subscription/screens/subscription-problem-screens';

/** Screen 16 — Subscription Expired (Phase 5 §5.16). Faith continues; nothing is deleted. */
export default function Screen() {
  return <SubscriptionExpiredScreen />;
}
