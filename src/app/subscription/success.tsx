import { PurchaseSuccessScreen } from '@features/subscription/screens/purchase-flow-screens';

/** Screen 07 — Purchase Success (Phase 5 §5.07). */
export default function Screen() {
  return <PurchaseSuccessScreen />;
}
