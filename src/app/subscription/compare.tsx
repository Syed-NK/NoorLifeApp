import { PlanComparisonScreen } from '@features/subscription/screens/plan-comparison-screen';

/** Screen 02 — Plan Comparison (Phase 5 §5.02). */
export default function Screen() {
  return <PlanComparisonScreen />;
}
