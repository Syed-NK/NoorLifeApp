import { FamilyInviteScreen } from '@features/subscription/screens/family-screens';

/** Screen 12 — Phase 5 §5.12. Premium Family only; six accounts including the organizer. */
export default function Screen() {
  return <FamilyInviteScreen />;
}
