import { FamilyPlanFullScreen } from '@features/subscription/screens/family-screens';

/** Screen 15 — Phase 5 §5.15. Premium Family only; six accounts including the organizer. */
export default function Screen() {
  return <FamilyPlanFullScreen />;
}
