import { FamilyInvitationsScreen } from '@features/subscription/screens/family-screens';

/** Screen 13 — Phase 5 §5.13. Premium Family only; six accounts including the organizer. */
export default function Screen() {
  return <FamilyInvitationsScreen />;
}
