import { FamilySetupScreen } from '@features/subscription/screens/family-screens';

/** Screen 11 — Phase 5 §5.11. Premium Family only; six accounts including the organizer. */
export default function Screen() {
  return <FamilySetupScreen />;
}
