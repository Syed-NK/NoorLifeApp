import { FamilyMembersScreen } from '@features/subscription/screens/family-screens';

/** Screen 14 — Phase 5 §5.14. Premium Family only; six accounts including the organizer. */
export default function Screen() {
  return <FamilyMembersScreen />;
}
